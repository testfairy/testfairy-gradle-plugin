language: android
sudo: false

branches:
  only:
    - master
    
jdk:
  - openjdk8
  
android:
  components:
    - android-25
    - build-tools-25.0.2

script:
  - bash gradlew uploadArchives
  - cd example/TestApplication; bash gradlew testfairyDebug

